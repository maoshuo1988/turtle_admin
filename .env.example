# 本地 max dev 的 /api 代理目标、以及 build 时写入前端的 API Base（与 UMI_APP_SERVER_API 二选一）
# 可选：staging | test | production
# UMI_APP_API_ENV=staging

# 构建产物里直接写死完整 API 根地址（优先级高于 UMI_APP_API_ENV）；示例：
# UMI_APP_SERVER_API=https://52.74.160.160
